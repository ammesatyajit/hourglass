import XCTest
@testable import Hourglass

/// Placeholder. The tester-agent will replace this with the real suite (fixture
/// chat.db, time-format disambiguation, attributedBody decode, contact merging).
final class HourglassTests: XCTestCase {
    func testScaffoldCompiles() {
        XCTAssertTrue(true)
    }
}
