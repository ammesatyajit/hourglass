# Multi-turn agent optimization — research synthesis + backlog + test loop

Goal: make Hourglass's on-device NL-search agent (the ReAct loop in
`Sources/NL/NLAgentReAct.swift`, running Qwen3-4B-4bit) more reliable. Grounded
in (a) the failures observed this session and (b) online research on multi-turn
tool-calling agents (2025-2026).

## Our observed failures (the eval record)
1. **Operator hallucination** — 1.7B/7B emitted invalid query operators (`with:me`, `type:chat`, `this_week`) → 0 results → gave up.
2. **Temporal hallucination** — "since September" → `in:"2026-09-01..2026-06-16"` (future month + backwards range). "what plans this week" off by wrong window.
3. **Repetition loop** — 7B emitted a 2,437-char query (`protein shake|shake recipe|…` ×80) → empty answer.
4. **Wrong tool / shallow** — "friends made since September" → `topContacts` (returned OLDEST friends); "plans this week" stopped at the first commitment.

## Research → mechanism → fix (mapped to our failures)

| Failure | Research finding | Our fix (on-device, no fine-tune) |
|---|---|---|
| Operator hallucination (1) | Tool schemas in the prompt are "advisory, not grammar-enforced" — exactly our prose setup. Grammar-constrained decoding masks invalid tokens. Validation gates: "reject, fix, or escalate — no silent failures." | **Validate tool args against the REAL operator grammar; on invalid, return a corrective observation naming valid operators** (turn a silent 0-result into a self-correcting signal). Gold standard = constrained decoding (bigger lift, later). |
| Temporal (2) | Models "fabricate plausible calendar mappings"; relative-date accuracy drops 23-35% vs absolute; prompt-based fixes are "marginally effective"; ISO-timestamp augmentation + deterministic resolution help; only post-training (DPO) fully fixes it. | **Resolve relative dates deterministically in code** ("since September" → most-recent-past Sept; "this week"; "N weeks ago") and **validate/clamp the model's date range** (reject future-relative-to-today, reject start>end) with a corrective observation. Don't trust the model's date math. |
| Repetition loop (3) | Repetition is self-reinforcing once started; "clear SUCCESS/terminal states cut tool calls 7×"; restrict/penalize repeated calls; anti-repetition prompt instructions. | We already have a **repeat-call breaker** (keep). Add a **degenerate-query guard**: reject a query that is absurdly long or self-repeats a token N× → corrective observation. |
| Wrong tool / shallow (4) | Validation gates; clear tool routing; bounded enums; "typed inputs, bounded enums drive consistency." | Purpose tools (`plansInWindow` ✓, `friendsMadeSince` TODO) + **bounded-enum operator presentation** + routing hints in the prompt. |

## ⚠️ Key NEGATIVE finding — do NOT add self-reflection
Reflexion/self-critique only helps at ~70B+. "For smaller models, self-reflexion can result in MORE erroneous responses — the limited capacity makes them suspect their original CORRECT answers and generate wrong ones." We run a 4B model. **Prefer deterministic guardrails over asking the model to reflect.** This rules out a tempting but harmful direction.

Corollary (research): for tool-calling, "architecture/training matters more than parameter count" — Qwen-family 4B benches ~97% on tool calls, beating bigger non-reasoning models. Matches our eval (Qwen3-4B > Qwen2.5-7B). So the model is fine; the **harness** is the lever.

## Prioritized backlog (highest leverage first; all deterministic)
- [x] **B1. Operator-validation gate.** ✅ DONE 2026-06-16 (`operatorCorrection(for:)` in NLAgentReAct, gating search/countMatching/firstMatching). Catches (1) `key=value` arg-injection (`limit=40`, `in=all_time` stuffed into the query) and (2) unknown `key:` operators; returns a corrective observation derived from `TokenPrefix.allCases` (can't drift from the parser). Turned the protein-shake query FAIL→PASS.
- [~] **B2. Date-range validation + relative resolution.** ✅ RELATIVE-RESOLUTION half DONE 2026-06-16: added calendar `TimeWindow` tokens (`last_month`/`this_month`/`last_week`/`this_week`/`yesterday`/`today`) resolved in code via `Calendar.dateInterval` (PlanJSON.toDateRange); prompt routes bare "last month/week" → calendar form, "last N days" → rolling; fixed the misleading photo example that taught `last:30d`. Turned the photo query NEAR-PASS→PASS (rolling→calendar). ✅ INCLUSIVE-END-DATE fix 2026-06-16: a date-only upper bound in an explicit `A..B` range anchored at 00:00:00, silently dropping the entire final day (`2026-05-01..2026-05-31` excluded all of May 31). `resolveDateArg` now extends a date-only upper to end-of-day. ✅ SINGLE-DATE fix 2026-06-16 (#29): a bare `YYYY-MM-DD` in the `in` arg fell through to nil (NO filter) → counted ALL messages (`in:"2026-06-14"` → 544,105 instead of ~371). resolveDateArg now resolves a single date to that whole calendar day. ✅ DATE-IN-CHAT-OP guard + FUTURE-DATE flag 2026-06-16: `operatorCorrection` now catches a date/range jammed into an `in:`/`chat:` operator and, when the date is in the future relative to `now`, tells the model it likely means the most-recent PAST year (the "since September" → 2026-09 mistake). ⬜ STILL OPEN: future-range CLAMP on the `in` ARG (vs. just flagging a date misplaced in the query); backwards-range (`lo>hi`) corrective (currently silently falls back to all-time).
- [~] **B3. Degenerate-query guard.** FINDING 2026-06-16 (#16): finally reproduced the repetition loop (the 4B repeating a synonym ×~50, e.g. `"lonely"|"lonely"|…` → 1179 chars) — but it BREAKS JSON parsing BEFORE `operatorCorrection` runs on the query string, so a query-string length/repeat guard can't catch it. It's already caught by the parse-fail→fallback safety net (no crash, just a degraded answer). MITIGATED at the source by prompt guidance (the topic carve-out tells the model: SHORT, ≤6 synonyms, NEVER repeat, bare keywords). A true generation-level (token) repetition guard is a bigger change — deferred; the fallback net + short-query guidance suffice. (Also surfaced: the `with:` footgun — a keyword in `with:`/`from:`/`to:` — not covered by the misplaced-keyword guard, which is `in:`/`chat:` only; the carve-out's "bare keywords, not with:" guidance avoids it on this path.)
- [ ] **B4. `friendsMadeSince` / `newContactsSince` tool** (contact-merged before/after-date volume split; excludes old friends w/ new handles — the Shreya case).
- [x] **B5. Bounded-enum operator values.** ✅ DONE 2026-06-16: `operatorCorrection` now validates `type:` VALUES against the real parser (`MessageSearch.TypeFilter.parse` → nil = invalid) — catches `type:message`/`type:chat` (valid prefix, invalid value → silently matched nothing → confidently-wrong zero). The operator catalog in the corrective is already derived from `TokenPrefix.allCases` (no prompt/parser drift). Caught `type:message` live; model self-corrected by dropping it. ✅ EXTENDED to `reactions:` 2026-06-16 (#25): validates reaction values via `ReactionFilter.parse` — caught `reactions:heart` (the ❤️ reaction is `love`; `heart` fell to free text → silent Count=0). Re-ran: guard → `reactions:love` → 27,135 (was 0). FAIL→PASS.

## Test set (ground truth, for the loop to score against)
| Query | Type | Expected | Source |
|---|---|---|---|
| `Couldn't even tell u if venkat in that or not` | keyword | exactly 1 result (Atul's msg) | regression: apostrophe+OR fix |
| `what plans did I commit to this week` | agentic | ≥4 of {NY trip, summer skills, chaat bhavan, LG brunch, boondocks 8am, hao dinner, finals}; no hallucination | docs/nl-eval-grounded.md |
| `what was that protein shake recipe i mentioned` | agentic | names the "800 cal / 60g protein" shake | nl-eval-grounded.md |
| `who were the friends i made since september` | agentic | NEW people (Saketh/Justin/Gandharva); NOT Shreya/Venkat/Beck (old, new handles) | this session |
| `who did I text the most in 2026` | agentic | correct topContacts ranking | — |
| `how many photos did I send last month` | agentic | a count via countMatching/type:image | — |
| `what messages did I send in july 2026` | agentic | 0 — July is the FUTURE (data ends 2026-06-16); answer truthfully, don't fabricate | this session (future-range probe) |
| `how many times did i mention gym in may 2026` | agentic | 3 (from:me "gym" in May); must NOT jam the keyword into in:/chat: | this session (in:-footgun, keyword variant) |
| `when did i first text beck` | agentic | 2024-11-20 UTC (Nov 19 22:30 local) via firstMatching with:"beck" | this session (firstMatching path) |
| `what have i been up to lately` | agentic | NAMES top contacts/groups (Beck/Annika/Venkat…) via topContacts/topGroups — NOT a bare message total | this session (open-ended routing) |
| `who did i text the most last week` | agentic | Beck (via topContacts in:last_week = CALENDAR week Jun 7–13; rolling-7d would wrongly give +1425…) | this session (last_week token) |
| `what did beck and i talk about recently` | agentic | readMessages on the Beck 1:1 → synthesize ACTUAL topics (not topContacts/volume) | this session (investigative path; #12 regression guard) |
| `what have i been stressed about lately` | agentic | search SHORT bare cue-words (stressed\|anxious\|…) → synthesize stressors — NOT topContacts, NOT a giant/repeating synonym chain | this session (topic carve-out; B3 finding) |
| `what is my most active group chat` | agentic | Hao group (topGroups in:all_time); un-timed superlative must default to ALL-TIME, not the current month | this session (topGroups; default-window) |

## Running results log (the loop appends here)
<!-- each loop fire: date · query · model answer · score · failure mode · optimization applied -->
- 2026-06-16 · `Couldn't even tell u if venkat in that or not` (keyword) · 1 result (Atul) · **PASS** · regression guard for apostrophe+OR fix · optimization: none (harness validation)
- 2026-06-16 · `who were the friends i made since september` (agentic) · NOW calls friendsMadeSince + date-clamp fired (2026-09→2025-09); returned Saketh/Justin/Karen/Annika; Beck (1260 before) excluded after tightening · **PASS (was topContacts→old friends)** · failure: wrong-tool + temporal-hallucination · **B4 applied** (friendsMadeSince tool, contact-merged before/after split, future-date clamp, maxBefore=250 cap). B2 partially baked in (date clamp). Threshold for Karen/Annika unverified by operator.
- 2026-06-16 · `what plans did I commit to this week` (agentic) · NOW uses plansInWindow → 63 msgs in ONE observation → named 5 distinct plans w/ citations (Venkat flights/NY ✓, Amma hand-brace, Beck Thu, tower next week, Gandharva) · **PASS (was: stopped at 1 commitment)** · failure: shallow-survey (fixed) · optimization: none — plansInWindow verified working live (built earlier, now confirmed)
- 2026-06-16 · `how many photos did I send last month` (agentic) · 169 via `from:me type:image last:30d` (ROLLING 30d); strict calendar-May ground truth = 154 · **NEAR-PASS** (right tool + filters; rolling-30d vs calendar-month window) · failure: temporal — "last month" → rolling 30d not calendar May · optimization: DEFERRED (B2 calendar-month resolution; disk at 99% — rebuild risk; also a genuinely ambiguous phrase). Refinement: teach `last:1mo`/"last month" → previous calendar month (after:YYYY-MM-01 before:next-01).
- 2026-06-16 · `what was that protein shake recipe i mentioned` (agentic) · BEFORE: iter1 `protein shake recipe`→0; iter2 broadened to `…|dinner|food|eat limit=40 in=all_time` (args stuffed in query, swallowed silently)→49 junk dinner rows→"no details provided" (**FAIL**). AFTER B1: iter2 gate fired ("ARGUMENTS stuffed into the query text: limit=40, in=all_time")→iter3 self-corrected to `protein|shake`→30 matches, [0]=the 800cal/60g shake→"800 calories and 60g protein", hero_index=0 ✓ · **FAIL→PASS** · failure: operator/arg-injection (silent) · **B1 applied** (`operatorCorrection` gate; corrective's worked example also nudged dropping the poisoning "recipe" term). Residual: synthesis added unrelated "protein bar/skyr" (precision, not operators).
- 2026-06-16 · `who did I text the most in 2026` (agentic) · iter1 `topContacts {in:2026-01-01..2026-12-31,limit:5}` → Beck Peterson 20,853 (10,227 sent/10,626 recv), Annika 7.9k, Venkat 5.4k → iter2 "Beck Peterson the most." Ground truth (raw chat.db): top 1:1 handle +15102196504 = 20,142 (= Beck's phone; next 1:1 only 8,095), Beck's email handle separately 2,304 → contact-merge → Beck clearly #1. · **PASS** · clean 2-turn: right tool + calendar-year range + contact-merge all correct. No failure → no optimization. (Note: range 2026-01-01..2026-12-31 is partly future but harmless — no data after today; B2 not needed here.)
- 2026-06-16 · `how many photos did I send last month` (agentic) RE-TEST · BEFORE (iter#3): `…type:image last:30d` + in:last_30d (ROLLING May17–Jun16) → 169, "in the last 30 days". AFTER B2: iter1 `{query:"from:me type:image", in:"last_month"}` (CALENDAR, dropped the redundant last:30d) → Count=146 → "146 photos last month." · **NEAR-PASS→PASS** · failure: temporal (rolling-vs-calendar window) · **B2 (relative-resolution half) applied** — calendar TimeWindow tokens resolved in code (last_month→[May1..May31] for a June now); prompt routes "last month"→calendar, "last N days"→rolling; killed the misleading `last:30d` example. Count shifted 169→146 because the window is now the actual calendar month.
- 2026-06-16 · `how many messages did I send in may 2026` (agentic; in:-footgun probe, truth ~6564 sent) · in:-footgun did NOT reproduce — model used `overviewStats {in:"2026-05-01..2026-05-31"}` (date in ARG, correct mechanism). BUT surfaced a confirmed bug: a date-only upper bound anchors at 00:00:00 → the closed range DROPPED all of May 31 (sent=6024). **B2 inclusive-end-date fix applied** (resolveDateArg extends a date-only upper to end-of-day). Re-ran → sent 6024→6180, total 24128→24849 (May 31 now included). · **NEAR-PASS→PASS** (every explicit A..B range was silently dropping its last day). Remaining gap to raw-SQL 6564 is legit (overviewStats excludes tapbacks + TZ). in:-footgun still latent for the countMatching path (not triggered when the model picks a stats tool).
- 2026-06-16 · `what messages did I send in july 2026` (agentic, future-range probe) · iter1 `{query:"from:me type:message in:\"2026-07-01..2026-07-31\"", in:"last_30d"}` → answer "0 messages in July 2026" (coincidentally CORRECT — July is future). But trace exposed TWO latent silent-zero bugs: (a) `type:message` is not a valid type value; (b) date range stuffed into the query's `in:` op (= chat-name filter), not the date arg. **B5 applied**: operatorCorrection validates `type:` values via TypeFilter.parse → caught `type:message` → model dropped it (iter2) → still "0 in July 2026" ✓. · **PASS (answer already correct; B5 removes a confidently-wrong-zero class)** · RESIDUAL (NEXT FIRE, top candidate): model puts a DATE RANGE into the query's `in:`/`chat:` operator instead of the `in` arg — on a real (non-future) month this would silent-zero. Fix: corrective when an `in:`/`chat:` value matches a date pattern → "dates go in the in-arg." B2-validation half (future-range clamp) NOT exercised here (the model never sent a future range to the date arg).
- 2026-06-16 · `how many times did i mention gym in may 2026` (agentic; in:-footgun countMatching probe, truth from:me=3) · BEFORE: iter1 `from:me in:"gym" type:text` (search KEYWORD jammed into the chat-name `in:` op) → 0 → iter2 malformed JSON → parse fail → **degraded to fallback, NO answer** (FAIL). AFTER misplaced-keyword guard: iter1 0 → guard fires ("NO search keyword; in:/chat: match CHAT NAMES; use bare text") → iter2 `from:me gym|gymnastics|exercise|workout` → 3 matches → iter3 arg-injection caught by B1 → iter4 "mentioned gym 3 times in May 2026" + 3 cited msgs ✓ EXACT · **FAIL→PASS** · failure: in:-footgun (keyword-in-chat-operator) cascading to malformed JSON/fallback · **misplaced-keyword guard applied** (search + countMatching: 0 results + in:/chat: token + no free-text → targeted corrective via the shared real tokenizer). This is the in:/chat: footgun (keyword variant) finally addressed; B1 also fired same trace (defense-in-depth).
- 2026-06-16 · `what have i been talking about since september` (agentic; B2 relative-date / date-in-chat-op probe) · BEFORE: model jammed a date range into the query's `in:` op AND used FUTURE 2026-09; then tried `since:` op + arg-injection → "no messages found" (FAIL). AFTER date-in-chat-op guard: iter1 guard fires on `in:"2026-09-01.."` → corrective (move date to the `in` arg; flags 2026-09 as FUTURE → use last year). Model THEN put the CORRECT range `2025-09-01..2026-06-16` in the ARG ✓ and removed the date from the query ✓ — but searched the literal phrase "since september 2025" as keywords → 0 (FAIL). · **DATE-HANDLING FIXED (footgun + year resolution), query still FAILS** · **date-in-chat-op guard applied** (operatorCorrection flags a date/range inside in:/chat: + a future-date note). REMAINING BLOCKER (next target, NOT a date bug): keyword-LESS open-ended "what have I talked about" — the model puts the time-phrase as keywords instead of issuing an empty-query / stats search. Needs a routing hint (open-ended summary → overviewStats/topGroups, empty query). FIRST loop query the optimization did not turn fully green.
- 2026-06-16 · `when did i first text beck` (agentic; firstMatching/oldestMatching path probe, truth 2024-11-20 UTC = Nov 19 22:30 local) · iter1 `firstMatching {query:"with:\"beck\"", in:"all_time"}` → first match Nov 19 2024 22:30 (Beck: "hey! on the off chance you're interested in psych research…") → iter3 "first text to Beck was November 19, 2024, at 22:30", hero_index=0 ✓ EXACT · **PASS** · clean: right tool + person-scope + local-time date + cited the actual first message. (iter2 had a harmless arg-injection but it was a DUPLICATE call → duplicate-breaker fired; B1 would have caught it too.) firstMatching path validated; no optimization.
- 2026-06-16 · `what have i been up to lately` (agentic; open-ended/keyword-less routing probe) · BEFORE: routed to overviewStats (good — NOT a keyword-flail) but answered bare VOLUME ("23,231 messages, 149 chats") — shallow, no WHO. AFTER open-ended routing hint: iter1 `topContacts {in:2026-06-01..2026-06-16, limit:5}` → Beck 1531, Annika 1044, Venkat 526, Noah 372, Keeshant 302 → "most active with Beck Peterson, Annika Renganathan, Venkat Chitturi…" NAMES the top people w/ counts ✓ · **NEAR-PASS→PASS** (shallow volume → substantive who-named) · failure: shallow-survey (#4) · **routing hint applied** (open-ended "what have I been up to / talking about / catch me up" → topContacts+topGroups, name people, not a bare total). Clean-window case ("lately"→recent range) now routes well; the date-math keyword-less case ("since september") still relies on the date-in-chat guard (#10).
- 2026-06-16 · `who did i text the most last week` (agentic; validates the last_week CALENDAR token from B2, never tested live) · iter1 `topContacts {in:"last_week", limit:5}` → Beck 693 (316s/377r), Venkat 377, Annika 289, Noah 261 → "Beck Peterson the most last week — 693 messages." · **PASS** · model chose last_week (CALENDAR, not rolling) → resolved to the prior calendar week Sun Jun 7–Sat 13 → Beck #1, matching the calendar-week ground truth (rolling-7d Jun 9–16 would instead crown +14253057121 at 877). Validates last_week/this_week tokens + the B2 "last week"→calendar routing end-to-end; contact-merge named Beck. No optimization.
- 2026-06-16 · `what did beck and i talk about recently` (agentic; investigative readMessages path + REGRESSION check on the #12 open-ended hint) · BEFORE: the #12 hint OVER-FIRED — a person-scoped "what did X talk about" matched the "what have I been talking about" pattern → `topContacts {in:last_month}` → "Beck is your top contact, 3232 messages" (VOLUME, wrong dimension; never read a message). FAIL. AFTER routing refinement: iter1 `readMessages {with:"Beck", in:2026-06-09..2026-06-16, limit:80}` → read 80 msgs → synthesized REAL topics (a Jacobian/stats homework-help thread + a personal health concern) with 18 evidence citations. · **FAIL→PASS** · failure: wrong-tool (#4), caused by the #12 hint over-generalizing to person-scoped queries · **routing refinement applied** (person-scoped "what did X and I talk about / what's going on with X" → readMessages/investigative, NOT topContacts; scoped the open-ended→topContacts rule to NO-person queries). Validates the investigative readMessages path end-to-end AND repairs the #12 regression.
- 2026-06-16 · `what have i been up to lately` (agentic; REGRESSION-CHECK of the #14 routing edit on the NO-person open-ended path) · iter1 `topContacts {in:2026-06-01..2026-06-16, limit:5}` → Beck 1531, Annika 1044, Venkat 526, Noah 372, Keeshant 302 → "most active with Beck Peterson, Annika Renganathan, Venkat…" (names the people; did NOT mis-route to readMessages). · **PASS (no regression)** · #14's "NO specific person named" qualifier correctly kept this no-person query on the topContacts path, while person-scoped "what did X talk about" goes to readMessages (#14). Both routing branches confirmed working side-by-side. No optimization.
- 2026-06-16 · `what have i been stressed about lately` (agentic; B3-trigger probe → routing+execution fix) · B3 probe found a MISROUTE first: model → topContacts ("most active with Beck, Annika", wrong dimension). Added a topic/feeling carve-out (→content search). v1 carve-out EXPOSED B3 LIVE: model used with:"stressed" (with:-footgun → 0) then spiraled into "lonely"×~50 → 1179 chars → JSON parse FAIL → fallback (research failure #3 reproduced). Refined the carve-out: "BARE keywords, ≤6 synonyms, NEVER repeat, not with:/chat:". Re-ran: iter1 `search stressed|stressful|anxious|overwhelmed|worried` → 31 matches → iter2 readMessages on Beck for context → iter3 "stressed about exams, a cancelled poster order, the move/unpacking, health/sleep" w/ citations. · **FAIL→PASS** (misroute → degenerate → substantive) · failure: wrong-tool (#4) + degenerate-query (#3) · **topic-routing carve-out applied** (feeling/topic → SHORT bare-keyword content search). B3: degenerate query breaks JSON before the query guard → caught by the fallback net; PREVENTED by short-query prompt guidance. with:-footgun noted (misplaced-keyword guard is in:/chat: only). Minor residual: answer mixes the user's stress with Beck's.
- 2026-06-16 · `what is my most active group chat` (agentic; topGroups path + default-window) · BEFORE: right tool (topGroups) but silently scoped to `in:2026-06-01..2026-06-16` (the CURRENT month) → "king cone and his conelings" (June's most active — WRONG for an un-timed query). AFTER default-window hint: iter1 `topGroups {in:"all_time"}` → Hao 36521 total #1, Survivors 28865, Securely Attached 14493 → "most active group chat is Hao." ✓ matches all-time ground truth. · **FAIL→PASS** · failure: temporal — an un-timed SUPERLATIVE defaulted to the current month, not all-time · **default-window hint applied** (superlative/ranking query with NO time qualifier → in:"all_time"; only narrow when the user names a window). Also validates the topGroups path end-to-end.
- 2026-06-16 · `who did i text the most last week` (agentic; REGRESSION-CHECK of the #17 default-window hint) · iter1 `topContacts {in:"last_week"}` → Beck 693 #1 → "Beck the most last week." · **PASS (no regression)** · the #17 "un-timed superlative → all_time" hint correctly did NOT over-apply: this TIMED superlative still uses in:last_week (the hint's "only narrow when the user names a window" clause holds). Confirms #17 is well-scoped; identical to the #13 result. Incremental regression-sweep of the accumulated routing hints — 1/N.
- 2026-06-16 · `what have i been up to lately` (agentic; REGRESSION-CHECK post #16 carve-out + #17 STATS edit) · iter1 `topContacts {in:"last_month"}` → Beck 3232, Annika 2435, Venkat 773, Linnea 696, Saketh 692 → "most active with Beck, Annika, Venkat, Linnea, Saketh in the last month." · **PASS (no regression)** · generic open-ended (NO person, NO topic) still → topContacts + names people; NOT pulled into #16's topic-search branch, and NOT switched to all_time by #17 (correctly — it's not a superlative, so a recent window is kept). Incremental regression-sweep 2/~4.
- 2026-06-16 · `how many times did i mention gym in may 2026` (agentic; REGRESSION-CHECK count query → found FLAKY) · #9 PASSED (3); this fire FAILED two ways. Run A: with:"gym" footgun (uncaught) → arg-injection → model COPIED `protein|shake` verbatim from the B1 corrective's EXAMPLE → searched `protein|shake|gym` → "12" (wrong). FIX: de-contaminated the corrective examples (concrete `protein|shake` / `from:me gym` → abstract `<your search words>` / `<thatWord>` placeholders). Run B (post-fix): no protein|shake copy ✓, BUT `with:"gym"` / `with:gym|gymnastics|…` footgun PERSISTS (misplaced-keyword guard is in:/chat: ONLY, not with:/from:/to:) → 0 → "no information" (FAIL). · **de-contamination CONFIRMED working; query still FLAKY** · **example de-contamination applied** · KEY: the with:-footgun (keyword in with:/from:/to:) — deferred in #17 as "low value" — is now shown to cause REAL failures. NEXT: extend the misplaced-keyword guard to with:/from:/to: with conditional "if X is a PERSON ignore; else write the word bare" phrasing (handles the with:"Xavier" false-positive).
- 2026-06-16 · `how many times did i mention gym in may 2026` (agentic; with:-footgun guard extension) · Extended the misplaced-keyword guard to `with:` (conditional phrasing: "if X is a real person, valid none-found; else write the word bare"; from:/to: EXCLUDED — from:me/to:me are common legit filters). CONFIRMED firing: iter3 `with:gym|gymnastics|exercise|workout|training` → 0 → with:-guard fired correctly. BUT this flaky run still FAILED: the model burned iter1 (`with:"gym" may 2026` — free-text "may 2026" blocks the no-free-text condition) + iter2 (arg-injection → B1) BEFORE the guard fired at iter3 = the read-cap (3) → forced answer iter4 → "did not mention gym" (WRONG; truth 3). · **with:-guard CONFIRMED correct (fires on the footgun); gym query still FLAKY** · failure: with:-footgun cascade exhausts the 3-call read-cap before any recovery turn · **with:-guard extension applied** · DEEPER ISSUE: recovery budget — the model cascades through 2-3 bad queries and the read-cap leaves no turn to act on the (correct, but late) corrective. Candidate next: raise the read-cap to ~5 so a late corrective can be acted on.
- 2026-06-16 · `what was that protein shake recipe i mentioned` (agentic; REGRESSION-CHECK of the FOUNDATIONAL B1 query, post all ~20 fires) · iter1 `protein shake recipe`→0 → iter2 broadened `protein shake recipe|protein shake|protein|shake|recipe|dinner|food|eat` → 12 matches incl [4] "I remembered this shake…800 cals and 60g protein" → iter3 readMessages Beck for context → iter4 "a protein shake recipe…around 800 calories and 60g of protein", hero_index=4 ✓ · **PASS (no routing/operator regression — B1 recall holds, finds the 800cal/60g shake)** · the #20 de-contamination did NOT break it (the query searches its OWN protein|shake, not the corrective's). MINOR nuance (NOT fixed — no clean B1–B5 mapping, stochastic): the answer attributed it to "Beck Peterson" but the message is `You:` (from:me) — chat-name confused with sender (#4 got it right). REGRESSION SWEEP COMPLETE (4/4): timed-superlative ✓ (#18), generic open-ended ✓ (#19), gym count = FLAKY (#20/#21, with:-guard added, read-cap is the residual), protein-shake recall ✓ (this).
- 2026-06-16 · `what did i tell beck about the gym` (agentic; sender-attribution probe — investigating the #22 nuance) · iter1 `search with:"Beck" gym` → 37 matches (mix of You:/Beck:) → iter2 readMessages Beck → iter3 "You told Beck about your past gym experiences… how hard YOU used to grind… YOU remembered the shake", hero_index=0 (a You: msg) · **PASS** · sender attribution CORRECT — the `You:` sent messages were attributed to "you", not Beck. The #22 "Beck mentioned" misattribution was a STOCHASTIC one-off, NOT reproducible here. DECISION: do NOT change the observation format (`chat="X" … Sender: body`) — there's no consistent failure to justify touching a format the model parses correctly the large majority of the time; a speculative reformat risks more than it fixes. Sender-attribution thread CLOSED as stochastic/rare.
- 2026-06-16 · `Couldn't even tell u if venkat in that or not` (keyword; model-free regression check, no build) · exactly 1 result: Atul's msg (curly apostrophe `Couldn't` preserved) · **PASS** · the foundational apostrophe-folding + OR-removal search fix still holds after ~23 agentic fires (search-engine code untouched this loop). TEST-SET COVERAGE NOW COMPLETE — every item in the test set verified this session.
- 2026-06-16 · `how many messages did i react to with a heart` (agentic; UNTESTED reactions: path) · BEFORE: `countMatching reactions:heart` → "heart" isn't a valid reaction kind → token falls to free text → Count=0 → "You reacted to 0 messages" (WRONG; 27,303 msgs have a love reaction). AFTER B5 reactions: validation: iter1 guard fired ("reactions:heart invalid — the ❤️ reaction is love") → iter2 arg-injection (B1) → iter3 `reactions:love` → Count=27,135 → "27,135 messages with a heart reaction." · **FAIL→PASS** (silent-0 invalid-operator fixed; 0→27,135) · failure: bounded-enum VALUE (B5 class) on reactions: · **B5 extended to reactions:** (ReactionFilter.parse, mirror of type:). LIMITATION (separate, out of scope): 27,135 = msgs WITH a love reaction (any direction); the literal "messages I reacted to" = 8,480 (love-reactions BY me) — the reactions: operator can't filter by WHO reacted (operator-capability gap, not a B5 issue).
- 2026-06-16 · `how many messages have 3 or more reactions` (agentic; UNTESTED reactions: COMPARATOR path) · iter1 `countMatching reactions:>=3` → Count=1385 → "1385 messages with 3 or more reactions." · **PASS** · the reactions: comparator (>=N) is parsed + used correctly (rough SQL ground truth: 1281 msgs with ≥3 add-reactions; ~8% gap = the app's removal-aware counting). Untested reactions:-comparator path VALIDATED, no optimization. (Untested-surface probing: #25 found a bug [reactions:heart], this validates reactions:>=3.)
- 2026-06-16 · `how many photos did beck send me` (agentic; UNTESTED from:OTHER + type: composition) · BEFORE: `from:"Beck" to:me type:image` → Count=0 → "Beck sent you 0 photos" (WRONG; from:"Beck" type:image ALONE = 313). The model added `to:me` to mean "received by me", but `to:X` = messages YOU SENT to X → from:Beck (received) + to:me (sent) = contradictory directions → 0. AFTER to:me guard: iter1 guard fired ("to:me does NOT mean received by you… drop it") → iter2 `from:"Beck" type:image` → 313 → "Beck sent you 313 images." · **FAIL→PASS** (0→313) · failure: to:me footgun (direction operator pointed at self) · **to:me guard applied** (operatorCorrection detects to:me/to:self/to:myself → corrective: use from:<person> alone). Untested-surface scorecard: #25 reactions:heart BUG, #26 reactions:>=3 ok, #27 to:me BUG (both fixed).
- 2026-06-16 · `how many photos did i send beck` (agentic; VALID to:PERSON direction + #27 guard false-positive check) · iter1 `from:me to:"Beck" type:image` → Count=481 → "You sent Beck 481 photos." EXACT (model-free ground truth: from:me to:"Beck" type:image = 481; with:"Beck" also 481) · **PASS** · validates the valid to:<person> direction (the legit counterpart to #27's to:me footgun) AND confirms the #27 to:me guard does NOT false-positive on to:"Beck" (it fires only on to:me/self/myself). No optimization. Untested-surface scorecard: 2 bugs (reactions:heart #25, to:me #27) + 2 validations (reactions:>=3 #26, to:PERSON #28).
- 2026-06-16 · `how many messages did i send on june 14 2026` (agentic; UNTESTED single-date `in` arg) · BEFORE: guards correctly rejected iter1 (`from:me type:message in:2026-06-14` → B5 type:message + date-in-chat), then iter2 `{query:"", in:"2026-06-14"}` → **Count=544,105** (the ENTIRE history!) — resolveDateArg handled only A..B ranges + TimeWindow tokens, so a bare single date fell through to nil = NO filter = ALL messages. AFTER B2 single-date fix (resolveDateArg resolves a bare YYYY-MM-DD → that whole calendar day): re-ran → Count=1265 (544,105→1265, day-scoped ✓). · **catastrophic all-time bug FIXED** · failure: B2 — single date in `in` arg → silent all-time · **B2 single-date resolution applied**. RESIDUAL (next): 1265 = ALL Jun-14 messages, not from:me (~371) — the model dropped from:me when the date-in-chat corrective told it to send an "empty query" (over-emptied, removed the valid from: operator too). Date-scoping fixed; from:me-drop is a separate corrective-cascade nuance.
- 2026-06-16 · `how many messages did i send on june 14 2026` (agentic; RE-TEST of the #29 from:me-drop residual) · BEFORE (#29): the date-in-chat corrective's "send query as empty" made the model drop from:me → `{query:"", in:"2026-06-14"}` → 1265 (ALL Jun-14 msgs). AFTER corrective reword ("Remove ONLY the date token; KEEP from:/to:/type:/with:; e.g. {query:\"from:me\", in:\"2026-06-14\"}"): iter2 `{query:"from:me", in:"2026-06-14"}` → Count=261 → "You sent 261 messages on June 14." from:me KEPT ✓ · **from:me-drop FIXED (1265→261, now from:me-scoped)** · failure: corrective-cascade over-emptying (fixed) · **date-in-chat corrective reworded** to preserve other operators. Across #29+#30: 544,105 → 1265 → 261. KNOWN NUANCE (logged, out of scope): 261 (in:-arg resolves in UTC) vs app on:2026-06-14 = 371 (local day) — a pre-existing UTC-vs-local boundary inconsistency between in:-arg date resolution (UTC) and the on: operator (local); would need a deliberate timezone audit across all date handling.
- 2026-06-16 · `how many links have i sent` (agentic; UNTESTED type:link value) · iter1 `countMatching from:me type:link` → Count=1321 → "You have sent 1321 links in total." EXACT (model-free ground truth: from:me type:link=1321) · **PASS** · type:link validated (clean 2-turn, exact match). No optimization. Untested-surface scorecard: 3 bugs (reactions:heart, to:me, single-date) + 3 validations (reactions:>=3, to:PERSON, type:link).
- 2026-06-16 · `how many photos did i send in the hao group` (agentic; UNTESTED in:chat + from:me + type: combo) · BEFORE: model used `to:"Hao group"` (PERSON op) for a GROUP CHAT → 0 → "0 photos" (truth: from:me in:"Hao" type:image = 372). **misplaced-keyword guard EXTENDED to to:** + group-chat guidance ("a group is scoped with in:\"<distinctive word>\", not to:/with:") + keep-other-operators + conditional escape (person-filter-gated, so it doesn't disturb in:/chat: cases like the gym recovery). · **guard CORRECT but query STILL FAILS** — across 4 corrective iterations the 4B made a DIFFERENT mistake each time: repeated to:"Hao group" → dropped from:me (in:"Hao"→2628) → used wrong value in:"Hao group" (→0, 'group' not in the chat name "Hao did this chat start") → used wrong operator to:"Hao" (→643). Never landed on from:me in:"Hao" type:image=372. · failure: wrong-operator-for-group (#4) + 4B multi-step-compliance limit · **to:/group-chat guard applied** (correct; helps simpler single-issue cases). RESIDUAL: this compound query (to:→in: AND "Hao group"→"Hao" distinctive-word AND keep from:me) exceeds the 4B's reliable multi-correction capacity — not a guard defect.
- 2026-06-16 · `how many videos have i sent` (agentic; UNTESTED type:video) · iter1 `countMatching from:me type:video` → Count=402 → "402 videos in total." EXACT (model-free ground truth from:me type:video=402) · **PASS** · type:video validated (clean 2-turn). No optimization. Untested-surface scorecard: 3 bugs (reactions:heart, to:me, single-date) + 4 validations (reactions:>=3, to:PERSON, type:link, type:video).
- 2026-06-16 · `how many of my messages got reactions` (agentic; from:me + reactions: combo) · iter1 `countMatching reactions:>=3` → 1385 → "1385 messages with 3+ reactions" (truth: from:me reactions:any = 12,873) · **FAIL** — model-INTERPRETATION errors: (a) read "got reactions" as reactions:>=3 (should be reactions:any / >=1), (b) DROPPED from:me (counted ALL messages, not "my"). NO clean deterministic fix: the operators are all VALID, the model just chose the wrong ones for the question (interpretation, not an operator-mechanism bug; doesn't map to B1-B5). Logged as a 4B query-interpretation limitation, NOT implemented (per discipline: don't force a speculative fix). Scorecard: 3 bugs fixed + 4 validations + this interpretation-FAIL (no deterministic fix).
- 2026-06-16 · `how many photos and videos have i sent` (agentic; multi-type OR combo) · BEFORE: `from:me type:image or type:video` → 19 (the word "or" is free TEXT, not boolean — only `|`/space-sep are OR; "or" required the body to contain "or"). truth: from:me type:image type:video (space=OR) = 4934. AFTER word-or guard: iter1 guard fired ("'or' is a literal WORD; SPACE-separate filters: type:image type:video; | is for free-text words only") → iter2 `from:me type:image type:video` → 4934 ✓ · **FAIL→PASS** (19→4934) · failure: operator-misuse (word "or" as boolean) · **word-or guard applied** (operatorCorrection: standalone "or" AND any operator present → corrective; gated on `sawOperator` so free-text "in that or not" [venkat] is untouched). Fixed within 2 builds — the 1st corrective wrongly suggested `type:image|type:video` (pipe → 0; | doesn't work between operators, only space-separation does) → caught + corrected per the #32 cap. Scorecard: 4 mechanism-bugs fixed + 4 validations.
- 2026-06-16 · `how many messages mention both gym and protein` (agentic; word-AND probe → instead hit with:-footgun-with-free-text) · model used `with:"gym" protein` (keyword "gym" jammed into with:; "protein" free text) → 0 (truth: gym protein = 5). The misplaced-keyword guard requires NO free text, so with:"gym" + free-text "protein" SLIPS THROUGH (3rd occurrence of with:-footgun-with-free-text, after #20/#21). NO word-and bug (the model never used "and" — space=AND is the default, so word-and is rarely triggered, unlike the natural "image or video"). · **FAIL** (0 vs 5) · failure: with:-footgun WITH free text (known gap) · NO fix forced — the clean fix needs a CONTACT-RESOLUTION check in the guard (resolve with: value → if no contact, treat as keyword), an async refactor of a pure static fn with fuzzy edges (a person can exist without a 1:1); deferred since #21 as a deliberate item. Relaxing the no-free-text condition would false-positive on legit with:"Person" topic → 0. Logged, not forced (#32/#34 discipline). Scorecard: 4 mechanism-bugs fixed + 4 validations + 2 deferred-unfixed (interpretation, with:-footgun-free-text).
- 2026-06-16 · `how many messages mention both gym and protein` (agentic; with:-footgun-with-free-text, 3rd occ.) · TWO coupled mechanism fixes. (1) **with:-contact-check**: on a 0-result search, if a `with:"X"` value matches NO contact (availableContactNames), HARD-correct ("with:\"gym\" matched no contact → write the word BARE"). Catches the footgun the no-free-text guard missed (`with:"gym" protein` has free text). (2) **dup-breaker recovery fix**: the duplicate-breaker recorded the signature of REJECTED (failed:true) calls BEFORE execution, so the model's CORRECTED retry collided with the rejected query → killed (the #21 recovery-budget root). Now only SUCCESSFUL calls are recorded. Re-ran: iter1 with:"gym"→contact-check → iter2 bare "gym protein limit=40…"→B1 (failed, NOT recorded) → iter3 "gym protein" → NOW RUNS → Found 5 ✓ (was blocked as dup). · **MECHANISM FIXED** (search 0→5, correct matches). RESIDUAL (synthesis, per #34 — not forced): the FORCED final answer said "2" (model re-examined the 5 previews and undercounted "explicitly mention both"). Both fixes confirmed; the dup-breaker fix broadly unblocks cascade recovery. Scorecard: 5 mechanism-bugs fixed (incl. dup-breaker recovery) + 4 validations.
- 2026-06-16 · `how many times did i mention gym in may 2026` (agentic; RE-TEST post recovery fixes) · The flaky gym query (#9 pass, #20/#21 FAIL — died to read-cap exhaustion after a cascade of REJECTED calls). #37 fixed the dup-breaker (don't RECORD rejected calls); THIS fixes the parallel READ-CAP issue (don't COUNT rejected calls toward the read-cap; bounded by maxIterations=8). Re-ran: iter1 with:"gym"→contact-check, iter2/3 arg-injection→B1 (3 rejected, now NOT counted), iter4 bare gym <date-as-text>→0, iter5 `from:me gym|exercise|workout` in:May → **Found 3** ✓ → iter7 "gym 3 times in May 2026" + the 3 msgs. · **FAIL→PASS** (flaky since #9, now passes reliably) · failure: read-cap counted REJECTED calls → a correctable cascade exhausted the budget before a clean query · **read-cap recovery fix applied** (count only successful reads). #37+#38 together FULLY fix the recovery-budget — the model can cascade through guard-corrections and land a clean query. Scorecard: 6 mechanism-bugs fixed (incl. dup-breaker + read-cap recovery) + 4 validations.
- 2026-06-16 · `how many photos did i send in the hao group` (agentic; RE-TEST post #37+#38 recovery fixes) · iter1 to:"Hao group" → #32 group-chat corrective → iter2 to:"Hao" → 643 → "643 photos to the Hao group" (truth: in:"Hao" type:image = 372) · **STILL FAIL (643 vs 372)** — same as #32's final outcome. The recovery fixes did NOT change this query: its failure is the to:→in: COMPLIANCE ceiling (the 4B uses to:"Hao" [person, 643] instead of in:"Hao" [group, 372]), NOT recovery-budget (the model answered cleanly at iter3, not force-stopped). NOT deterministically fixable: operatorCorrection sees only the search query (not the user's "group" intent), and to:"Hao" returns valid results so no guard fires. Confirms the #32 multi-step-compliance limit; the recovery fixes are correctly well-targeted (fixed the gym query #38 whose issue WAS budget; correctly don't paper over this). No fix forced (#32/#34). Scorecard unchanged: 6 mechanism-bugs + 4 validations.
- 2026-06-16 · `what was that protein shake recipe i mentioned` (agentic; REGRESSION-CHECK foundational B1+recovery, post #37/#38 + 16 fires) · iter1 `protein shake recipe`→0 → iter2 broaden→12 incl [4] the 800cal/60g shake → iter3 readMessages → iter4 (read-cap hit after 3 SUCCESSFUL reads) "800 calories and 60g of protein", hero_index=4 ✓ · **PASS (no regression)** · CONFIRMS the recovery fixes are regression-safe: the read-cap still counts SUCCESSFUL reads correctly (3→forced); #38 only spares REJECTED calls; dup-breaker unchanged for successful calls. (Same minor stochastic sender-attribution nuance as #22 — "Beck mentioned" for a You: msg — core answer correct.) Scorecard unchanged: 6 mechanism-bugs + 4 validations + 2 model-capacity FAILs.
- 2026-06-16 · `how many messages mention gym in any form` (agentic; *substr* validation + search-as-count finding) · (1) `*substr*` VALIDATED: model-free `*gym*`=1071 > `gym`=995 (wildcard matches gymnastics etc.); multi-term `|` OR also correct (`gym|exercise|workout|fitness|training`=1571). (2) The AGENTIC answer was WRONG ("38 messages"): the model recovered from a with:"gym" footgun (contact-check ✓) to bare `gym|exercise|workout|fitness|training`, but used `search` (limit:40 → a CAPPED 38-result sample) for a COUNT question and reported the sample size as the total (true total ~1571). · **FAIL (38 vs ~1571)** — root: tool-choice (used search not countMatching for "how many"; the prompt already says use countMatching) + the "Found N" observation reads like a total when it's a limit-capped sample. NOT forced: largely interpretation (#34); the deterministic angle (warn when results.count==limit) wouldn't fire here (38≠40) and needs investigating the cap behavior (deliberate). Candidate: add a "capped — use countMatching for the exact total" warning when a search hits its limit. Scorecard: 6 mechanism-bugs + 5 validations (incl *substr*) + 3 model-capacity/interpretation FAILs.
- 2026-06-16 · `show me some messages where i mention food` (agentic; cap-warning test → surfaced a DEEPER FTS-OR bug) · Added a cap-warning (search results.count >= limit → "SAMPLE not total; use countMatching") — but it did NOT fire: the agentic search returned only **8** for `food|dinner|eat|cook|recipe|bake|meal|snack` (model-free `food` alone = 1545). · **DEEPER BUG FOUND**: the agentic `tools.search` routes through FTS (shouldUseFTS) with a limit; FTS returned 8 (should be ≤40 by recency) for the 8-term `|` OR. SEARCH_EVAL calls `search.search(phrase:)` UNLIMITED (INSTR?) → 1545. So the **FTS path UNDER-RETURNS multi-term `|` OR queries** (8/38 [#41] vs 1545/1571 model-free) — an FTS-vs-INSTR OR-translation discrepancy in FTSSearcher. Affects EVERY OR/synonym search the model does (concept searches under-return). NOT fixed: deep FTS-layer work, DELIBERATE scope (#32). The cap-warning is a correct preventive guard (kept) but addresses a different [limit-capped single-term] case. NOTE: countMatching is UNAFFECTED (uses fts.aggregateCount, exact). Scorecard: 6 mechanism-bugs + 5 validations + 3 interpretation/capacity FAILs + 1 NEW deliberate bug (FTS-OR under-return).
- 2026-06-16 · (investigation, no new query) FTS multi-term-OR under-return (from #42) — ROOT-CAUSE NARROWED · Traced the 8-vs-1545 discrepancy through the search layer: (a) PhraseQuery parses `food|dinner|…` into ONE OR-group (correct — `|`→OR within a group, spaces→AND across groups); (b) FTSSearcher.buildFTS5Expression correctly emits `("food" OR "dinner" OR …)` (groups OR-within, AND-across); (c) so the bug is in FTS5 TRIGRAM MATCH EXECUTION of the quoted-phrase OR — same OR-group gives 1545 via INSTR but 8 via FTS5. NOT the AST/grouping. This is FTS5-trigram-specific (quoted-phrase OR under-matching), diagnosable only by testing FTS5 MATCH directly against the index; a blind search-layer fix risks breaking ALL queries → DELIBERATE scope (#32). CANDIDATE SAFE MITIGATION: route multi-term-OR queries (any AST group with >1 needle) to the INSTR path (which works) instead of FTS — needs perf + correctness testing across query types. countMatching UNAFFECTED (aggregateCount exact). No code change. Scorecard unchanged: 6 mechanism-bugs + 5 validations + 3 interpretation/capacity FAILs + 1 deliberate FTS bug (now characterized).
- 2026-06-16 · (mitigation attempt FAILED — corrects the #43 diagnosis) Tried routing multi-term-OR queries to INSTR (`useFTS = shouldUseFTS() && !query.contains("|")`) to fix the OR under-return. Re-ran the food query: STILL 8 (and the search step took 16.8s — so INSTR DID run, slowly, and ALSO returned 8). But model-free `food|dinner|eat|cook|recipe|bake|meal|snack` = 4528 (INSTR ORs correctly). So **my #43 "FTS-specific" root-cause was WRONG**: the agentic `tools.search` under-returns OR via BOTH backends (FTS=8 AND INSTR=8), while SEARCH_EVAL's unlimited `search.search` = 4528. The real difference is `tools.search` (limit:40, order:.descending, possibly a JOIN/LIMIT/ordering interaction) vs SEARCH_EVAL's unlimited path — root cause STILL UNKNOWN, deeper than thought. REVERTED the mitigation (it made the query slower without fixing it). Genuinely DELIBERATE scope — needs direct instr.search/SQL investigation, not a per-fire fix. No net code change. countMatching unaffected. Scorecard: 6 mechanism-bugs + 5 validations + 3 interpretation/capacity FAILs + 1 deliberate bug (agentic-search-OR under-return, root cause OPEN).
- 2026-06-16 · (OR-bug narrowing + INVESTIGATION CAPPED) Further localized the agentic-search OR under-return: SEARCH_EVAL uses `viewModel.messageSearch.search(phrase:)` (→4528); tools.search uses `tools.instr`/`tools.fts` (→8). MessageSearch has ONE `search` func (line 177, full params + defaults), so it's NOT an overload divergence — it's that tools.instr/tools.fts are DIFFERENT search objects than viewModel.messageSearch. Exact root cause needs direct testing of each search object (deliberate). CAPPING the OR investigation after 4 fires (#42 found, #43 mis-diagnosed FTS, #44 corrected [both backends], #45 narrowed) — well past the #32 over-investment line; it's a clearly-scoped deliberate item now, not a per-fire fix. No code, no new query (read-only investigation). DELIBERATE ITEMS: (1) agentic-search OR under-return [tools.instr/fts vs viewModel.messageSearch divergence], (2) timezone audit. Scorecard unchanged: 6 mechanism-bugs + 5 validations + 3 interp/capacity FAILs + 1 deliberate (OR under-return).
- 2026-06-16 · `how many voice messages have i sent` (agentic; type:audio + voice→audio alias) · `countMatching from:me type:voice` → Count=29 → "29 voice messages." EXACT (ground truth from:me type:audio=29) · **PASS** · type:audio validated; model used the natural "voice" alias → TypeFilter.parse maps voice→audio; B5 correctly did NOT flag it (valid alias). No optimization. Pivoted off the (capped, deliberate-scope) OR bug. Scorecard: 6 mechanism-bugs + 6 validations + 3 interp/capacity FAILs + 1 deliberate (OR under-return) + 1 deliberate (timezone).
- 2026-06-16 · `what was the conversation around 11pm on june 14 2026` (agentic; validates messagesAroundTime + summarizeArgs dup-signature fix) · messagesAroundTime TOOL works (returns msgs around a timestamp). BUG found: summarizeArgs (the duplicate-call signature) only included query/in/date/limit/guid → a 2nd messagesAroundTime with a different chat_id (1419 vs null) was FALSE-FLAGGED as a duplicate → blocked the re-zoom. Also affects readMessages with a different `with` person (same date). FIX: summarizeArgs now includes with/person/chat_id/before/after/since + stringifies int args. Re-ran: iter2 chat_id:1419 NOW RUNS → zoomed into the Hao group → richer answer. · **FAIL→PASS (dup-signature)** · failure: incomplete duplicate signature → false duplicates blocking legit different calls · **summarizeArgs fix applied**. Validates the last untested REACHABLE tool. (context tool is UNREACHABLE — needs a GUID, never exposed in formatResultLine.) RESIDUAL: model passed "11pm"→23:00Z UTC = 4pm local (deferred TIMEZONE issue). Scorecard: 7 mechanism-bugs + 7 validations.
- 2026-06-16 · `catch me up on beck and annika` (agentic; VERIFY the #47 summarizeArgs fix on the impactful readMessages multi-person case) · iter1 `readMessages with:"Beck"` → 80 msgs (Jacobian/stats) → iter2 `readMessages with:"Annika"` (SAME date range, DIFFERENT person) → NOW RUNS (was false-flagged as a duplicate pre-#47) → 80 msgs (Klemm meeting/flight/study) → iter3 synthesizes BOTH ("Beck discussed a Jacobian… Annika planned a Klemm meeting…") · **PASS** · confirms the #47 dup-signature fix generalizes from messagesAroundTime to the impactful case: multi-person investigative queries ("catch me up on X and Y") now read EACH person instead of being blocked on the 2nd. No optimization. Scorecard: 7 mechanism-bugs + 7 validations.
- 2026-06-16 · `how many documents have i sent` (agentic; type:file) · `countMatching from:me type:file` → Count=1602 → "1602 documents." EXACT (ground truth from:me type:file=1602) · **PASS** · type:file validated. Type-value coverage now COMPLETE: image/video/audio/link/file all ✓ (only type:sticker untested — identical pattern). No optimization. Scorecard: 7 mechanism-bugs + 8 validations.
- 2026-06-16 · `how many stickers have i sent` (agentic; type:sticker — LAST untested type value) · `countMatching from:me type:sticker` → Count=451 → "451 stickers." EXACT (ground truth 451) · **PASS** · type:sticker validated. TYPE-VALUE COVERAGE 100% COMPLETE: image/video/audio/link/file/sticker all ✓. No optimization. Scorecard: 7 mechanism-bugs + 9 validations + 3 interp/capacity FAILs + 2 deliberate.
